#!/usr/bin/env bash
# ML-Guided Optimization Pass Selector Executable Launcher
# Usage: ./run_optimizer tests/sample.c --strategy ml

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
export PYTHONPATH="$DIR:$PYTHONPATH"

python3 "$DIR/src/main.py" "$@"
