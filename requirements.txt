# Machine-Learning-Guided LLVM Optimization Pass Selector
# Compiler Design Lab Project A26 (Ubuntu 22.04 LTS + LLVM 14)

# Core Requirements:
# The core ML framework, feature extractor, pass runner, and visualizer
# are implemented in pure Python (Python 3.8+) with ZERO external dependencies required!
# Standard library modules used: os, sys, math, random, json, csv, re, subprocess, statistics, itertools.

# Optional Enhancement Packages (for extended plotting and scikit-learn validation):
numpy>=1.21.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
pandas>=1.3.0
